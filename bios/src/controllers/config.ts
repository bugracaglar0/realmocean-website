const config = {
    databaseId: 'your-real-database-id',
    databaseName: 'your-database-name',
    organizationId: 'your-organization-id',
    // Diğer yapılandırma bilgileri buraya eklenebilir
};

export default config;
